export default function ({ params, store, redirect }) {
  // If the region is not found from the API call, redirect to the landing page
  // if (!slugs.includes(params.region)) {
  //   return redirect('/landing')
  // }
}
